# ToDos

- Postgres Image + Run + Init DB scripts vs Postgres with Init DB scripts Image + Run
- How to write enums in Typescript (enum + isInstanceOf + assertIsInstanceOf + guardFalse + UnreachableNotSupportedEnumException)
